<footer class="site-footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-text">
        <p class="footer-name">Wiam EL Khammal</p>
        <p class="footer-tagline">Software Engineer</p>
       
      </div>
      <div class="footer-social">
        <a href="mailto:elkhammalwiam@gmail.com" aria-label="Email">
          <i class="fa-solid fa-envelope"></i>
        </a>
        <a
          href="https://www.linkedin.com/in/wiam-el-khammal-84ab21257/"
          target="_blank"
          rel="noopener"
          aria-label="LinkedIn"
        >
          <i class="fa-brands fa-linkedin"></i>
        </a>
        <a
          href="https://github.com/WiamELKhammal"
          target="_blank"
          rel="noopener"
          aria-label="GitHub"
        >
          <i class="fa-brands fa-github"></i>
        </a>
        
        <a
          href="https://www.codingame.com/profile/0a0ea4aaeddda8f52a3b6a66976aa79e5734507"
          target="_blank"
          rel="noopener"
          aria-label="CodinGame"
        >
          <i class="fa-solid fa-code"></i>
        </a>
        <a
          href="https://leetcode.com/u/Tweetyw/"
          target="_blank"
          rel="noopener"
          aria-label="LeetCode"
        >
          <i class="fa-solid fa-code-branch"></i>
        </a>
      </div>
    </div>
    <div class="footer-bottom">
  <p class="footer-copy">
    <span class="matrix-text">©</span> <span id="year"></span> 
    <span class="code-comment">//</span> 
    <span class="matrix-fade">Crafted in</span> 
    <span class="hex-code" title="Morocco in hex">0x4D 0x41</span> 
    🇲🇦 
    <span class="code-comment">with</span> 
    <span class="matrix-heart">&lt;passion/&gt;</span> 
    <span class="code-operator">&&</span> 
    <span class="matrix-coffee">🍵</span>
  </p>
</div>
  </div>
</footer>

<!-- Back to top button -->
<button class="to-top" aria-label="Back to top">↑</button>

<script>
  // Set current year
  const yearSpan = document.getElementById("year");
  if (yearSpan) {
    yearSpan.textContent = new Date().getFullYear().toString();
  }

  // Back to top functionality
  document.addEventListener("DOMContentLoaded", () => {
    const toTopBtn = document.querySelector(".to-top");

    if (toTopBtn) {
      // Show/hide button based on scroll position
      window.addEventListener("scroll", () => {
        if (window.scrollY > 300) {
          toTopBtn.classList.add("show");
        } else {
          toTopBtn.classList.remove("show");
        }
      });

      // Scroll to top when clicked
      toTopBtn.addEventListener("click", () => {
        window.scrollTo({
          top: 0,
          behavior: "smooth",
        });
      });
    }
  });
</script>

<style>
  /* Footer styles */
  .site-footer {
    background: var(--bg-elev);
    border-top: 1px solid var(--border);
    padding: 3rem 0 2rem;
    margin-top: 4rem;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .footer-text {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .footer-name {
    font-size: 1.25rem;
    font-weight: 700;
    margin: 0;
    color: var(--text);
  }

  .footer-tagline {
    font-size: 0.9rem;
    color: var(--muted);
    margin: 0;
  }

  .footer-roles {
    margin: 0;
    margin-top: 0.3rem;
    font-size: 0.7rem;
    color: var(--muted);
    opacity: 0.8;
  }

  .role-link {
    color: inherit;
    text-decoration: none;
    border-bottom: 1px dotted transparent;
    transition: all 0.2s ease;
  }

  .role-link:hover {
    color: var(--text);
    border-bottom-color: var(--text);
  }

  .role-link:focus-visible {
    outline: 1px solid var(--text);
    outline-offset: 2px;
  }

  .role-sep {
    margin: 0 0.4rem;
    opacity: 0.5;
  }

  .footer-social {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .footer-social a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--muted);
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .footer-social a:hover {
    border-color: var(--text);
    color: var(--text);
    transform: translateY(-2px);
  }

  .footer-social a:focus-visible {
    outline: 2px solid var(--text);
    outline-offset: 2px;
  }

  .footer-bottom {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid var(--border);
  }

  .footer-copy {
    margin: 0;
    color: var(--muted);
    font-size: 0.9rem;
    font-family: 'Courier New', monospace;
    letter-spacing: 0.02em;
  }

  /* Matrix-style geek elements */
  .matrix-text {
    color: #00ff41;
    font-weight: bold;
    text-shadow: 0 0 5px rgba(0, 255, 65, 0.5);
    animation: matrix-flicker 3s infinite;
  }

  .code-comment {
    color: #888;
    font-style: italic;
  }

  .matrix-fade {
    color: var(--muted);
    opacity: 0.9;
  }

  .hex-code {
    color: #00d9ff;
    font-weight: 600;
    background: rgba(0, 217, 255, 0.1);
    padding: 0.1rem 0.3rem;
    border-radius: 2px;
    border: 1px solid rgba(0, 217, 255, 0.2);
    cursor: help;
    transition: all 0.3s ease;
  }

  .hex-code:hover {
    background: rgba(0, 217, 255, 0.2);
    border-color: rgba(0, 217, 255, 0.4);
    transform: scale(1.05);
  }

  .matrix-heart {
    color: #ff006e;
    font-weight: bold;
    animation: pulse-glow 2s ease-in-out infinite;
  }

  .code-operator {
    color: #ffd700;
    font-weight: bold;
  }

  .matrix-coffee {
    display: inline-block;
    animation: steam 2s ease-in-out infinite;
  }

  /* Matrix flicker animation */
  @keyframes matrix-flicker {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
  }

  /* Pulse glow animation for heart */
  @keyframes pulse-glow {
    0%, 100% { 
      text-shadow: 0 0 5px rgba(255, 0, 110, 0.5);
      transform: scale(1);
    }
    50% { 
      text-shadow: 0 0 10px rgba(255, 0, 110, 0.8);
      transform: scale(1.1);
    }
  }

  /* Steam animation for coffee */
  @keyframes steam {
    0%, 100% { 
      transform: translateY(0) rotate(0deg);
      opacity: 1;
    }
    50% { 
      transform: translateY(-2px) rotate(5deg);
      opacity: 0.8;
    }
  }

  /* Light theme adjustments */
  :global([data-theme="light"]) .matrix-text {
    color: #00cc33;
    text-shadow: 0 0 3px rgba(0, 204, 51, 0.3);
  }

  :global([data-theme="light"]) .hex-code {
    color: #0088cc;
    background: rgba(0, 136, 204, 0.1);
    border-color: rgba(0, 136, 204, 0.2);
  }

  :global([data-theme="light"]) .hex-code:hover {
    background: rgba(0, 136, 204, 0.2);
    border-color: rgba(0, 136, 204, 0.4);
  }

  :global([data-theme="light"]) .matrix-heart {
    color: #e60073;
  }

  :global([data-theme="light"]) .code-operator {
    color: #cc9900;
  }

  /* Light theme */
  :global([data-theme="light"]) .footer-social a {
    background: transparent;
    color: var(--muted);
  }

  :global([data-theme="light"]) .footer-social a:hover {
    border-color: var(--text);
    color: var(--text);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .footer-content {
      flex-direction: column;
      text-align: center;
    }

    .footer-text {
      align-items: center;
    }

    .footer-social {
      justify-content: center;
    }

    .footer-copy {
      font-size: 0.8rem;
      line-height: 1.6;
    }

    .hex-code {
      font-size: 0.85rem;
    }

    .footer-roles {
      font-size: 0.65rem;
    }
  }

  @media (max-width: 480px) {
    .footer-copy {
      font-size: 0.75rem;
      word-spacing: 0.1em;
    }
  }

  /* Touch devices */
  @media (hover: none) and (pointer: coarse) {
    .footer-social a:hover {
      transform: none;
      border-color: var(--border);
      color: var(--muted);
    }

    .footer-social a:active {
      transform: scale(0.95);
      border-color: var(--text);
      color: var(--text);
    }
  }

  /* Reduce motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    .matrix-text,
    .matrix-heart,
    .matrix-coffee {
      animation: none;
    }

    .hex-code:hover {
      transform: none;
    }
  }
</style>
